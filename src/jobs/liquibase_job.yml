description: >
  The job will run a database migration with Liquibase

executor: default

parameters:
  changelogFile:
    type: string
    default: "changelog.xml"
    description: "changelog file"
steps:
  - lb_commands:
      changelogFile: << parameters.changelogFile >>
